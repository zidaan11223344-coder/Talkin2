# حزمة نشر البوت — مضيف الغراب

هذه الحزمة لا تحتوي على مستودع Git أو ملفات الكاش أو ملفات الأغاني المؤقتة أو ملفات الأسرار المحلية. ملفات البيانات الموجودة داخل `data/` هي حالة البوت القابلة للنقل.

## التشغيل

1. ارفع محتويات الحزمة إلى المنصة.
2. ثبّت الحزم من `requirements.txt`.
3. أضف المتغيرات من `.env.example` و`.env.master.example` إلى Secrets/Environment Variables في المنصة.
4. اضبط `BOT_ID` و`BOT_PWD` و`BOT_MASTER` و`TELEGRAM_BOT_TOKEN` عند الحاجة.
5. شغّل `python bot.py` أو استخدم `Dockerfile`.

## رابط صور الهدايا

يجب أن تفتح المنصة منفذاً HTTP عاماً للخدمة، وأن تمرر رقم المنفذ في المتغير
`PORT`. خادم البوت يجيب الآن بـ `200 OK` على المسارات `/` و`/health` و`/healthz`
حتى ينجح فحص صحة المنصة، ويخدم صور الهدايا من المسار `/gifts/`. إذا كانت المنصة
لا تضيف رابطاً عاماً تلقائياً، أضف الرابط الكامل للخدمة في متغير
`ALGHORAB_PUBLIC_URL` أو `PUBLIC_BASE_URL`، مثل:

```text
ALGHORAB_PUBLIC_URL=https://اسم-الخدمة.example.com
```

لا تستخدم رابط لوحة التحكم أو رابط WebSocket؛ المطلوب هو رابط HTTP العام الذي
يصل إلى نفس الخدمة والمنفذ الذي يشغله البوت.

## التخزين

يُنصح بربط مجلد دائم للمسار `data/` حتى لا تضيع النقاط والغرف والتوثيقات عند إعادة النشر. يمكن تفعيل مزامنة ملفات البيانات مع مستودع Talkin4 عبر `GITHUB_SYNC=1` و`GITHUB_TOKEN`.

## المتغيرات

أمر `انسخ احتياطي` يرسل ملف `railway_variables.env` إلى Telegram فقط عند طلبه يدويًا. لا ترفع هذا الملف إلى مستودع عام، ولا تشاركه خارج محادثة خاصة.

## التنظيف

التنظيف التلقائي يحذف مخلفات الصوت والكاش القديمة دون حذف `data/` أو `bot_data/`.
