# تحديث البوت — 2026-09-14

## دخول الغرف المحظورة

عند إرسال `دخول اسم_الغرفة` لغرفة حُظر منها البوت، لا تظهر رسالة «الغرفة مسجلة بالفعل». يرسل البوت الآن رسالة واضحة تطلب إعطاءه إشرافاً أو أونر ثم إعادة المحاولة.

## حماية الغرف

يمكن للماستر أو **صانع/مالك الغرفة فقط** استخدام الأوامر التالية داخل الغرفة. المشرف العادي لا يملك صلاحية تشغيل أو إيقاف الحماية:

- `تشغيل الحماية` لتفعيل حماية التكرار في الغرفة.
- `إيقاف الحماية` لإيقافها.
- `mr@عدد` لتحديد عدد الرسائل المتطابقة المتتالية قبل الحظر، والحد الافتراضي 3، مع حد آمن من 2 إلى 50.

عند الوصول إلى الرسالة قبل الأخيرة يرسل البوت تحذيراً. وعند تجاوز الحد يحظر المستخدم ويرسل سبب الحظر: «تكرار مشبوه». إعدادات كل غرفة تحفظ في `moderation.json` ولا تؤثر على الغرف الأخرى.

## فلتر الكلمات

عند تطابق كلمة موجودة في الفلتر، يحظر البوت المستخدم ويرسل رسالتين معنويتين في إشعار واحد: اسم المستخدم وسبب «كلمة مسيئة». تبقى أوامر إدارة كلمات الفلتر الحالية للماستر.

## خدمة الماستر

تشغيل الماستر لم يعد يجعله يدخل الغرف المحفوظة أو الغرفة الافتراضية. يبقى حساب الماستر متصلاً بخدمة الخاص فقط لاستقبال طلبات التوثيق والشكاوى والتوثيق لحساب آخر.

## بطاقة الهدية

تم تكبير القالب عمودياً، وأصبح يحتوي على مستطيلين واضحين للمرسل والمستقبل، مع عرض صورة كل طرف بجانب مستطيله وليس داخله عند توفرها.

## حالة ملف البوت عند إرسال الهدية

بعد نجاح إرسال هدية من داخل الغرفة، تتغير حالة ملف البوت مؤقتاً إلى تفاصيل المرسل والمستقبل ونوع الهدية لمدة 15 دقيقة. عند إرسال هدية أخرى تُلغى الحالة السابقة وتُستبدل بتفاصيل الهدية الجديدة. بعد انتهاء المدة تعود الحالة إلى قيمة `BOT_BASE_STATUS`، ويمكن اختيار فعل تحديث الملف عبر `PROFILE_STATUS_ACTION` عند اختلاف إصدار خادم Talkin.

## القوائم

تمت إضافة أوامر الحماية إلى قائمة الماستر والفلتر في صفحات المساعدة، مع إبقاء قائمة الألعاب مجمعة في صفحة الألعاب.

## التحقق

- فحص الصياغة: ناجح لملفات `bot.py` و`master_bot.py`.
- اختبار الحماية ودخول الغرفة المحظورة: ناجح.
- اختبار بطاقة الهدية: ناجح.
- بعض اختبارات الألعاب القديمة تتطلب تهيئة `room` في كائن الاختبار ولا تصل إلى منطق التعديل.

> يجب إعادة تشغيل البوت بعد رفع الملفات، والتأكد من أن حساب البوت يملك صلاحية إدارة الغرفة حتى يستطيع تنفيذ الحظر والترقية.
